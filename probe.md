# probe

_Temporary file, created for build ticket #20._

A build ticket is "specified fully enough that an unattended agent can carry it
out with no one to ask". "One branch per build ticket." "Never edit a file
another build ticket owns." "Do not commit to `main`, and do not merge your own
pull request." Done: "the named test passes".

Quoted from `CONTEXT.md` and `CLAUDE.md`.
